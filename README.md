# `psort`: Characteristics-sorted Portfolios in R

## Installation

You can install the released version of `psort` from [Github](https://github.com/) with:

``` r
devtools::install_github("bilnash/psort")
```
